# 本地组件扩展验收记录 · 2026-09-09

本轮在 Windows x64 上完成 124 项新增操作的逐项实际媒体执行，以及两个注册组件的空目录自动安装后续跑。总工具目录为 172 项。尚未注册 Wan-Animate 等重型 AI 执行组件。

| 检查 | 实际结果 |
| --- | --- |
| 新增操作 | 图片 36、视频 48、音频 40，共 124/124 实际执行成功 |
| FFmpeg 自动安装 | 从空组件目录下载 111253802 字节归档，校验 SHA-256、解压、运行 FFmpeg/ffprobe，再继续原视频作业 |
| Sharp 自动安装 | 从空组件目录使用锁文件安装原生预编译依赖，实际图片处理成功，无用户安装动作 |
| HTTP 作业 | 图片与视频均自动完成组件准备、执行、验证和原任务成果登记 |
| MCP / CLI | 用真正的 MCP stdio 和 CLI 提交、读取图片/音频作业；幂等重复请求返回同一 job |
| 进程重启 | 中断隔离服务器、启动新进程后，原作业和关联计划节点成功完成 |
| 下载故障 | 临时失败自动重试同一作业；另覆盖断点、错误 Range、坏 SHA、不可信重定向、安装锁和旧版保留 |
| 输入保护 | 同路径更换素材不会复用旧结果；执行前后检查源文件；成果使用独立目录 |
| 效果检查 | 纯绿幕铺黑后 RGB 均值 0；44.1 kHz 的 440 Hz 音频按 1.1 倍变调后约 483.08 Hz、时长约 0.998 秒；32×24 图片边框保持尺寸 |
| 浏览器 | Edge 实际页面；1440×1000 与 390×844；124 张目录卡片、历史任务进度、成果链接、刷新保留、无页面 JS 异常、无横向溢出 |
| 后端回归 | 827 项中的 825 项通过、0 失败、2 项探测后跳过；两项边界帧用例随后定向 2/2 通过 |
| 前端回归 | 145/145，通过生产构建 |
| 插件回归 | 51/51，包括 SQLite 任务/节点/事件/偏好的搬迁、升级和重启保留 |

回执见 [逐项公开清单](../backend-node/components/acceptance-windows-x64.json)，完整操作和参数见 [组件使用说明](LOCAL-MEDIA-COMPONENTS.md)。公开清单保留实际输入/输出摘要、字节、版本、参数和日期，并绑定执行器源码摘要；没有复制本机用户名、源素材路径、数据库或凭据。修改执行器后，旧的实测标记会失效；Windows 与 Git 的换行转换按 LF 规范化比较。

验收程序在 `backend-node/scripts`：`accept-local-media.cjs` 为逐项输出矩阵，`accept-local-effects.cjs` 为上述数值效果检查，`serve-local-media-acceptance.cjs` 运行隔离数据库/存储，`accept-local-protocol.cjs` 测 MCP/CLI，`accept-local-recovery.cjs` 测真实进程重启。它们需要显式指定隔离目录，不读取正式 Key，不调用收费生成。

证据范围：124 项使用短时生成样本；对每项检查实际执行、输出可解码、源文件保留，另对代表性效果做数值检查。该证据不表示所有长视频、所有参数组合、复杂遮挡、真人转动漫或身份一致性修复均已通过。跨平台、重模型和大规模素材压力仍应按各自组件补齐实机验收。
