# 本地组件扩展验收记录 · 2026-09-09

## 2026-09-10 候选增量

最新代码实际执行了203项本地操作：201项基础Sharp/FFmpeg操作、源视频关键帧与故事板提取、镜头重剪。全部产生可读取输出并保留源文件，公开回执已更新到当前执行器指纹。基础操作包括71项图片、70项视频及60项音频，其短样本证据独立于下面的内容场景；OCR、PDF和超分使用各自真实素材验收，不能用基础滤镜样本代替。

新增的 `video_reverse_prepare` 与 `video_reverse_compile` 已通过真正的MCP工具发现和调用；实际18秒源视频提取24帧、6张无缩放故事板、30个可下载附件，Codex逐板查看后编译结构化分镜提示词。相同请求复用原job、中断恢复后原成果可读。安装器复制到全新隔离目录后，两个Skill入口与新MCP工具仍可发现，并能读取已有作业。语义描述和最终视频效果保持独立核对，抽帧/编译不证明角色替换合格。

新增 `local.video.edit-timeline` 用已有30秒真实动态商品素材选择五段镜头并替换旁白，本机持久作业约16.27秒完成15秒、720×1280、24fps、H264/AAC成片。工具拒绝越界/倒序时间范围及比成片更长的旁白，保留用户指定的镜头排列顺序。最终浏览器完整1倍速播放360帧、零丢帧；产品外观和对应细节镜头已检查，ASR辅助确认四句完整。人工听感未确认，不宣称投放转化已通过；本地重剪也不等于另一个Seedance2.0原生15秒请求成功。

本轮发布前回归：后端877项、前端159项、MCP及更新/搬迁恢复57项、依赖锁检查1项通过；前端构建通过。桌面及390宽手机页面可见两个反推工具、6张故事板和原帧下载，没有横向溢出。真实素材和完整本机收据保留在本机，公共仓库只携带脱敏验证清单。

12个实际图片/音视频场景复现并修复了普通图片操作忽略EXIF方向，以及单纯镜像画面仍重编码音乐的问题。修复后12项通过：方向归一化后缩放、补边、显式旋转、裁剪；同款商品完整适配、透明图铺白、WebP；镜像、截取、变速、音轨导出和单声道。安装组件复用时，这批小样的单作业耗时为1.422至8.193秒。音乐保留用AAC压缩包哈希核对；兼容编码逐轨复制，不兼容轨转AAC，改变时间的操作仍处理音频。

实际Real-ESRGAN原生推理中断试验：终止隔离后台后未发现残留推理进程；重启后原job自动恢复，23.892秒完成2048方图，组件复用，16条既有作业和14个既有任务保留，同请求和成果不重复。该结果仅覆盖本机这次进程中断，不能推断所有系统或显卡驱动故障。

新增成果内容核对与文件有效性区分，沿用反馈接口保存可追溯的接受/修改结论；原文件和技术检查不变。真实浏览器1440及390宽度验证了图标入口、修改说明、提交失败保留草稿、刷新后结论保留和无横向溢出。该功能记录核对，不自动提交生成或收费。

普通中文可搜索PDF任务经注册执行器一次完成，作业8.63秒，普通任务启动到首PDF约142秒，下载和原图嵌入数据核对通过，33条OCR均可搜索。小字存在识别错误；MuPDF搜索框与OCR框约0.002px以内，另一提取器因字体度量不同有3至8px顶部差异，不能声称各阅读器逐像素一致。

真实普通任务发现无计划节点时PDF已成功但任务无法完成。候选完成逻辑现读取活动节点及独立本地作业，原任务无需重做即可收口，重复完成保持幂等和原成果。随后一个新的真实图片作业861ms完成，任务摘要自动由running到succeeded，未创建计划节点。内容核对状态保持独立。

任务列表新增可恢复归档与恢复：默认列表隐藏归档项，原链接、素材和运行状态保留，归档不取消供应商请求。桌面1440与手机390实际点击、刷新、临时503失败与恢复通过。无节点任务的完成摘要也已实际检查。

最新定向编排/作业/PDF34项通过，执行器18项证据另存。完整上游视频效果、所有普通用户宿主任务和所有硬件情况不在此证据范围内。此处为候选验收记录，不代表已经发布或替换用户运行中的后台。

## 早期发布批次

本轮在 Windows x64 上完成 124 项新增操作的逐项实际媒体执行，以及两个注册组件的空目录自动安装后续跑。总工具目录为 172 项。尚未注册 Wan-Animate 等重型 AI 执行组件。

| 检查 | 实际结果 |
| --- | --- |
| 新增操作 | 图片 36、视频 48、音频 40，共 124/124 实际执行成功 |
| FFmpeg 自动安装 | 从空组件目录下载 111253802 字节归档，校验 SHA-256、解压、运行 FFmpeg/ffprobe，再继续原视频作业 |
| Sharp 自动安装 | 从空组件目录使用锁文件安装原生预编译依赖，实际图片处理成功，无用户安装动作 |
| HTTP 作业 | 图片与视频均自动完成组件准备、执行、验证和原任务成果登记 |
| MCP / CLI | 用真正的 MCP stdio 和 CLI 提交、读取图片/音频作业；幂等重复请求返回同一 job |
| 进程重启 | 中断隔离服务器、启动新进程后，原作业和关联计划节点成功完成 |
| 下载故障 | 临时失败自动重试同一作业；另覆盖断点、错误 Range、坏 SHA、不可信重定向、安装锁和旧版保留 |
| 输入保护 | 同路径更换素材不会复用旧结果；执行前后检查源文件；成果使用独立目录 |
| 效果检查 | 纯绿幕铺黑后 RGB 均值 0；44.1 kHz 的 440 Hz 音频按 1.1 倍变调后约 483.08 Hz、时长约 0.998 秒；32×24 图片边框保持尺寸 |
| 浏览器 | Edge 实际页面；1440×1000 与 390×844；124 张目录卡片、历史任务进度、成果链接、刷新保留、无页面 JS 异常、无横向溢出 |
| 后端回归 | 827 项中的 825 项通过、0 失败、2 项探测后跳过；两项边界帧用例随后定向 2/2 通过 |
| 前端回归 | 145/145，通过生产构建 |
| 插件回归 | 51/51，包括 SQLite 任务/节点/事件/偏好的搬迁、升级和重启保留 |

回执见 [逐项公开清单](../backend-node/components/acceptance-windows-x64.json)，完整操作和参数见 [组件使用说明](LOCAL-MEDIA-COMPONENTS.md)。公开清单保留实际输入/输出摘要、字节、版本、参数和日期，并绑定执行器源码摘要；没有复制本机用户名、源素材路径、数据库或凭据。修改执行器后，旧的实测标记会失效；Windows 与 Git 的换行转换按 LF 规范化比较。

验收程序在 `backend-node/scripts`：`accept-local-media.cjs` 为逐项输出矩阵，`accept-local-effects.cjs` 为上述数值效果检查，`serve-local-media-acceptance.cjs` 运行隔离数据库/存储，`accept-local-protocol.cjs` 测 MCP/CLI，`accept-local-recovery.cjs` 测真实进程重启。它们需要显式指定隔离目录，不读取正式 Key，不调用收费生成。

证据范围：124 项使用短时生成样本；对每项检查实际执行、输出可解码、源文件保留，另对代表性效果做数值检查。该证据不表示所有长视频、所有参数组合、复杂遮挡、真人转动漫或身份一致性修复均已通过。跨平台、重模型和大规模素材压力仍应按各自组件补齐实机验收。
