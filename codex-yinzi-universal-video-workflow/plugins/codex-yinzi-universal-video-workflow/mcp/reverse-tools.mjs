const context = { session_id: { type: 'string' }, request_key: { type: 'string' }, node_key: { type: 'string' }, goal: { type: 'string' } }
export const reverseTools = [
  { name: 'video_reverse_prepare', description: '反推视频第一步：自动抽取带真实时间的原尺寸关键帧和故事板，返回持久作业。用 local_media_get_job 取成果，Codex 必须查看画面后继续 video_reverse_compile。无需用户安装组件或配置视觉模型。', inputSchema: { type: 'object', required: ['session_id', 'request_key', 'input_path', 'goal'], properties: { ...context, input_path: { type: 'string' }, frame_count: { type: 'integer', minimum: 2, maximum: 120, default: 24 }, start: { type: 'number', minimum: 0 }, end: { type: 'number', minimum: 0 }, timestamps: { type: 'array', items: { type: 'number' }, maxItems: 120 } } } },
  { name: 'video_reverse_compile', description: '反推视频第二步：提交 Codex 实际看图后的镜头动作、镜头、背景和转场描述；工具校验帧时间与实际上传参考索引，生成可直接提交的视频提示词和验收时间表。保持待生成/待验收真实状态，不调用付费模型。', inputSchema: { type: 'object', required: ['session_id', 'request_key', 'manifest_path', 'observations'], properties: { ...context, manifest_path: { type: 'string' }, observations: { type: 'array', minItems: 1, maxItems: 120, items: { type: 'object', required: ['start', 'end', 'frame_ids', 'description'], properties: { start: { type: 'number' }, end: { type: 'number' }, frame_ids: { type: 'array', items: { type: 'string' } }, description: { type: 'string' }, camera: { type: 'string' }, background: { type: 'string' }, transition: { type: 'string' }, audio: { type: 'string' }, uncertainties: { type: 'string' } } } }, references: { type: 'array', items: { type: 'object', required: ['type', 'index', 'role'], properties: { type: { enum: ['image', 'video', 'audio'] }, index: { type: 'integer', minimum: 1 }, role: { type: 'string' }, frame_ids: { type: 'array', items: { type: 'string' } } } } }, preserve: { type: 'string' }, avoid: { type: 'string' }, output_duration: { type: 'number' }, timing_mode: { enum: ['preserve', 'retime'] }, retime_instruction: { type: 'string' }, max_prompt_chars: { type: 'integer', minimum: 100, maximum: 32000, default: 8000 } } } },
]
